# AI-driven Jira/Bitbucket SDLC pipeline: plan → implement → create-pr → poll-pr
#
# All tasks run as Kubernetes pods using the ai-dev-tools image.
# Each task gets a fresh emptyDir at /workspace; artifacts pass data between tasks.
# host_network: true enables Tailscale VPN access to the Bedrock proxy.
#
# Credentials are injected from the 'ai-dev-credentials' Kubernetes secret
# (created once by deploy-ai-jira-workflows.sh --create-k8s-secret).
# Non-secret config is set via org configs (deploy-ai-jira-workflows.sh --set-configs).
#
# Deploy (with Formicary running in Kubernetes via k8s.yaml):
#   ./deploy-ai-jira-workflows.sh --create-k8s-secret --set-configs \
#     --jira-project MYPROJ --bb-workspace myworkspace --bb-repo myrepo

job_type: ai-jira-implement
description: "AI agent: plan → implement → create-pr → poll-pr for a Jira issue"
max_concurrency: 5
timeout: 86400s

variables:
  IssueNumber:
    type: STRING
    required: true

tasks:
# --- PLAN --------------------------------------------------------------------
- task_type: plan
  method: KUBERNETES
  timeout: 15m
  report_stdout: true
  host_network: true
  working_dir: /workspace
  container:
    image: plexobject/ai-dev-tools:latest
    volumes:
      empty_dir:
        - name: workspace
          mount_path: /workspace
    env_from:
      - secret_ref: ai-dev-credentials
  environment:
    WORKSPACE_DIR: /workspace
    JIRA_BASE_URL: "{{.JiraUrl}}"
    JIRA_PROJECT: "{{.JiraProject}}"
    BITBUCKET_WORKSPACE: "{{.BitbucketWorkspace}}"
    BITBUCKET_REPO: "{{.BitbucketRepo}}"
    MAX_TURNS_PLAN: "{{.MaxTurnsPlan}}"
    GIT_USER_NAME: "{{.GitUserName}}"
    GIT_USER_EMAIL: "{{.GitUserEmail}}"
    BASE_BRANCH: "{{.GitBranch}}"
    CLAUDE_CODE_USE_BEDROCK: "{{.ClaudeUseBedrock}}"
    ANTHROPIC_BEDROCK_BASE_URL: "{{.AnthropicBedrockBaseUrl}}"
    CLAUDE_CODE_SKIP_BEDROCK_AUTH: "{{.ClaudeSkipBedrockAuth}}"
    AI_MODEL: "{{.AnthropicSonnetModel}}"
    ANTHROPIC_DEFAULT_SONNET_MODEL: "{{.AnthropicSonnetModel}}"
    ANTHROPIC_DEFAULT_OPUS_MODEL: "{{.AnthropicOpusModel}}"
    ANTHROPIC_DEFAULT_HAIKU_MODEL: "{{.AnthropicHaikuModel}}"
  script:
    - python -m scripts.jira.issue_picker --issue-id {{.IssueNumber}}
    - python -m scripts.jira.plan --issue-id {{.IssueNumber}}
  artifacts:
    paths:
      - ./issue.json
      - ./plan.md
      - ./plan_result.json
      - ./logs
    expire_after: 24h
  on_exit_code:
    2: PAUSE_JOB    # BLOCKED — needs human intervention
  on_completed: implement

# --- IMPLEMENT ---------------------------------------------------------------
- task_type: implement
  method: KUBERNETES
  timeout: 45m
  report_stdout: true
  host_network: true
  working_dir: /workspace
  container:
    image: plexobject/ai-dev-tools:latest
    volumes:
      empty_dir:
        - name: workspace
          mount_path: /workspace
    env_from:
      - secret_ref: ai-dev-credentials
  dependencies:
    - plan
  environment:
    WORKSPACE_DIR: /workspace
    JIRA_BASE_URL: "{{.JiraUrl}}"
    JIRA_PROJECT: "{{.JiraProject}}"
    BITBUCKET_WORKSPACE: "{{.BitbucketWorkspace}}"
    BITBUCKET_REPO: "{{.BitbucketRepo}}"
    MAX_TURNS_IMPLEMENT: "{{.MaxTurnsImplement}}"
    GIT_USER_NAME: "{{.GitUserName}}"
    GIT_USER_EMAIL: "{{.GitUserEmail}}"
    BASE_BRANCH: "{{.GitBranch}}"
    CLAUDE_CODE_USE_BEDROCK: "{{.ClaudeUseBedrock}}"
    ANTHROPIC_BEDROCK_BASE_URL: "{{.AnthropicBedrockBaseUrl}}"
    CLAUDE_CODE_SKIP_BEDROCK_AUTH: "{{.ClaudeSkipBedrockAuth}}"
    AI_MODEL: "{{.AnthropicSonnetModel}}"
    ANTHROPIC_DEFAULT_SONNET_MODEL: "{{.AnthropicSonnetModel}}"
    ANTHROPIC_DEFAULT_OPUS_MODEL: "{{.AnthropicOpusModel}}"
    ANTHROPIC_DEFAULT_HAIKU_MODEL: "{{.AnthropicHaikuModel}}"
  script:
    - python -m scripts.jira.implement --issue-id {{.IssueNumber}}
  artifacts:
    paths:
      - ./impl_result.json
      - ./branch.txt
    expire_after: 24h
  on_completed: create-pr

# --- CREATE-PR ---------------------------------------------------------------
- task_type: create-pr
  method: KUBERNETES
  timeout: 10m
  report_stdout: true
  host_network: true
  working_dir: /workspace
  container:
    image: plexobject/ai-dev-tools:latest
    volumes:
      empty_dir:
        - name: workspace
          mount_path: /workspace
    env_from:
      - secret_ref: ai-dev-credentials
  dependencies:
    - plan
    - implement
  environment:
    WORKSPACE_DIR: /workspace
    JIRA_BASE_URL: "{{.JiraUrl}}"
    JIRA_PROJECT: "{{.JiraProject}}"
    BITBUCKET_WORKSPACE: "{{.BitbucketWorkspace}}"
    BITBUCKET_REPO: "{{.BitbucketRepo}}"
    INPROGRESS_LABEL: "{{.InProgressLabel}}"
    PR_OPEN_LABEL: "{{.PrOpenLabel}}"
    SLACK_CHANNEL: "{{.SlackChannel}}"
    SLACK_CHANNEL: "{{.SlackChannel}}"
    CLAUDE_CODE_USE_BEDROCK: "{{.ClaudeUseBedrock}}"
    ANTHROPIC_BEDROCK_BASE_URL: "{{.AnthropicBedrockBaseUrl}}"
    CLAUDE_CODE_SKIP_BEDROCK_AUTH: "{{.ClaudeSkipBedrockAuth}}"
    AI_MODEL: "{{.AnthropicSonnetModel}}"
    ANTHROPIC_DEFAULT_SONNET_MODEL: "{{.AnthropicSonnetModel}}"
    ANTHROPIC_DEFAULT_OPUS_MODEL: "{{.AnthropicOpusModel}}"
    ANTHROPIC_DEFAULT_HAIKU_MODEL: "{{.AnthropicHaikuModel}}"
  script:
    - python -m scripts.jira.create_pr --issue-id {{.IssueNumber}}
  artifacts:
    paths:
      - ./pr.json
    expire_after: 24h
  on_completed: poll-pr

# --- POLL-PR -----------------------------------------------------------------
# poll_pr.py runs once: exits 0 (merged/declined), 3 (still open → PAUSED), 1 (error).
# learn.py is called automatically inside poll_pr on exit 0.
# delay: controls how long formicary waits before re-running this task after PAUSED.
- task_type: poll-pr
  method: KUBERNETES
  report_stdout: true
  host_network: true
  working_dir: /workspace
  container:
    image: plexobject/ai-dev-tools:latest
    volumes:
      empty_dir:
        - name: workspace
          mount_path: /workspace
    env_from:
      - secret_ref: ai-dev-credentials
  dependencies:
    - create-pr
    - poll-pr
  environment:
    WORKSPACE_DIR: /workspace
    JIRA_BASE_URL: "{{.JiraUrl}}"
    BITBUCKET_WORKSPACE: "{{.BitbucketWorkspace}}"
    BITBUCKET_REPO: "{{.BitbucketRepo}}"
    NEEDS_HUMAN_LABEL: "{{.NeedsHumanLabel}}"
    MAX_TURNS_FEEDBACK: "{{.MaxTurnsFeedback}}"
    SLACK_CHANNEL: "{{.SlackChannel}}"
    CLAUDE_CODE_USE_BEDROCK: "{{.ClaudeUseBedrock}}"
    ANTHROPIC_BEDROCK_BASE_URL: "{{.AnthropicBedrockBaseUrl}}"
    CLAUDE_CODE_SKIP_BEDROCK_AUTH: "{{.ClaudeSkipBedrockAuth}}"
    AI_MODEL: "{{.AnthropicSonnetModel}}"
    ANTHROPIC_DEFAULT_SONNET_MODEL: "{{.AnthropicSonnetModel}}"
    ANTHROPIC_DEFAULT_OPUS_MODEL: "{{.AnthropicOpusModel}}"
    ANTHROPIC_DEFAULT_HAIKU_MODEL: "{{.AnthropicHaikuModel}}"
  script:
    - python -m scripts.jira.poll_pr --issue-id {{.IssueNumber}}
  artifacts:
    paths:
      - ./pr.json
      - ./monitor_result.json
      - ./processed_comments.json
    expire_after: 168h    # 7 days
  on_exit_code:
    3: PAUSE_JOB    # PR still open — formicary waits delay then retries
  on_completed: done
  delay: "{{.PollInterval}}s"

# --- DONE --------------------------------------------------------------------
- task_type: done
  method: SHELL
  script:
    - echo "Pipeline complete"

job_variables:
  MaxTurnsPlan: "50"
  MaxTurnsImplement: "200"
  MaxTurnsFeedback: "20"
  PollInterval: "120"
  GitUserName: "AI Agent"
  GitUserEmail: "ai-agent@noreply.local"
  GitBranch: "main"
  InProgressLabel: "ai-in-progress"
  PrOpenLabel: "ai-pr-open"
  NeedsHumanLabel: "needs-human"
  SlackChannel: ""
  ClaudeUseBedrock: "1"
  AnthropicBedrockBaseUrl: "http://ai/bedrock"
  ClaudeSkipBedrockAuth: "1"
  AnthropicSonnetModel: "us.anthropic.claude-sonnet-4-6"
  AnthropicOpusModel: "us.anthropic.claude-opus-4-6-v1"
  AnthropicHaikuModel: "us.anthropic.claude-haiku-4-5-20251001-v1:0"
  # Injected via org configs (set via deploy-ai-jira-workflows.sh --set-configs):
  #   JiraUrl, JiraProject, BitbucketWorkspace, BitbucketRepo  (plain)
  #   SlackChannel             (plain, optional — Slack notifications)
  # Injected from the 'ai-dev-credentials' K8s secret:
  #   JIRA_EMAIL, JIRA_API_TOKEN, BITBUCKET_USERNAME, BITBUCKET_TOKEN
  #   SSH_PRIVATE_KEY, ANTHROPIC_API_KEY (optional)
  #   SLACK_BOT_TOKEN          (optional — enables Slack notifications)
  # NOTE: Two separate username vars are required:
  #   BITBUCKET_USERNAME     — account email (e.g. user@example.com) for REST API Basic Auth
  #   BITBUCKET_GIT_USERNAME — account nickname for git HTTPS clone/push
